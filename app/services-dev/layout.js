export const metadata = {
  title: 'Services Dev - React WebApp',
  description: 'Development page for services teaser section',
}

export default function ServicesDevLayout({ children }) {
  return children;
}